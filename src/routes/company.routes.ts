// import { Router } from "express";

// import { Company as CompanyController } from "../controllers/company.controller";
// import { authorization } from "../middlewares/authorization";
// import { upload } from "./index";

// export const router = Router();

// router.get("/", CompanyController.getCompanies);
// router.get("/:id", CompanyController.getCompanyById);

// router.use(authorization);

// router.post("/", upload.single("profileImage"), CompanyController.createCompany);
// router.patch("/:id", upload.single("profileImage"), CompanyController.updateCompanyById);
// router.delete("/:id", CompanyController.deleteCompanyById);
